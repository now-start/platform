spring:
  cloud:
    decrypt-environment-post-processor:
      enabled: false
    discovery:
      enabled: false
    gateway:
      server:
        webflux:
          discovery:
            locator:
              enabled: false
    config:
      enabled: false
  security:
    user:
      name: 'basic-admin'
      password: '{bcrypt}$2a$10$Y0HAJQ/lEa/63WX7RuNr.eBIVKUOgr9QH6C/IKL.Es4jvXnabr7XO'
      roles:
        - 'ADMINISTRATORS'
    oauth2:
      client:
        registration:
          test:
            client-id: "test-id"
            client-secret: "test-secret"
            authorization-grant-type: "authorization_code"
            redirect-uri: "{baseUrl}/login/oauth2/code/{registrationId}"
        provider:
          test:
            authorization-uri: "http://localhost:9999/oauth/authorize"
            token-uri: "http://localhost:9999/oauth/token"
            jwk-set-uri: "http://localhost:9999/oauth/jwks"

eureka:
  client:
    enabled: false
